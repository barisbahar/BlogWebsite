    <a href="/addplace" class="btn-floating btn-large waves-effect waves-light red" style="position:fixed; bottom:40px; right:40px" ><i class="material-icons">add</i></a>
    {{#if places.length}}
    <div>
    {{#each places as |place|}}
<div class="row">
    <div class="col s12 m6">
      <div class="card">
        <div class="card-image">
          <a href="/placeinfo/{{_id}}"> <img src={{{image}}} ></a>
          <span class="card-title">{{{title}}}</span>
          <a class="btn-floating halfway-fab waves-effect waves-light red" href="/editplace/{{_id}}"><i class="material-icons">edit</i></a>
        </div>
        <div class="card-content">
          <p>{{{description}}}</p>
        </div>
      </div>
    </div>
  </div>
    {{/each}}
    </div>

{{else}}
<p>No places!!</p>
{{/if}}
